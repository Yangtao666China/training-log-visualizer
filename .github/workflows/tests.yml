name: Tests
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Install
        run: python -m pip install ".[example]"
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Reproduce example
        run: |
          python examples/generate_runs.py
          python -m trainlens "sample_runs/*.csv" --output outputs/report.html
